export enum LocalStorageEnum {
    USER = "user",
    TOKEN = "Token",
    CALL_HISTORY = "Call_history",
    APP_SETTINGS = "App_settings",
    LAST_VIEWED_HEALTH_TIP = "last_viewed_health_tip"
}

export enum AddOrRemoveAction {
    ADD = "Add",
    REMOVE = "Remove"
}